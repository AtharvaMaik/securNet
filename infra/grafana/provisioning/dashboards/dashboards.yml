apiVersion: 1

providers:
  - name: security-observability-platform
    folder: Security Observability
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
